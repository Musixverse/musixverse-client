# This file contains environment variables that are used by the development environment but need not be secret
# These are required to run a development server locally on a developer's system.

NEXT_PUBLIC_IPFS_NODE_URL="https://ipfs.moralis.io:2053/ipfs/"